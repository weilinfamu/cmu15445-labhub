TrieNode() = default;
语法说明：
= default 表示编译器自动生成默认构造函数。

默认构造会初始化 children_ 为空，is_value_node_ 为 false。

======================================================================================================================
✅ 构造函数（带子节点）
explicit TrieNode(std::map<char, std::shared_ptr<const TrieNode>> children)
  : children_(std::move(children)) {}


======================================================================================================================

